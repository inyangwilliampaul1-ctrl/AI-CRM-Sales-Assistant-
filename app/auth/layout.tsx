/**
 * Auth Callback Layout
 * 
 * Minimal layout for the auth callback page.
 * Prevents the main app layout from interfering with the callback.
 */
export default function AuthCallbackLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
